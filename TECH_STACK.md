# Технологический стек YellowTip Reservations

## Архитектура приложения

**Архитектурный паттерн:** Full-Stack TypeScript приложение с разделением на Frontend и Backend

---

## 🎨 Frontend (Client-Side)

### Основные технологии
- **React 19.2.0** — UI библиотека
- **TypeScript 4.9.5** — типизированный JavaScript
- **React Router DOM 7.9.4** — маршрутизация

### UI Framework и библиотеки
- **Material-UI (MUI) 7.3.4** — компонентная библиотека
  - `@mui/material` — основные компоненты
  - `@mui/icons-material` — иконки
  - `@mui/x-date-pickers 8.15.0` — календари и выбор даты/времени
- **Emotion** — CSS-in-JS решение (используется MUI)

### State Management и Data Fetching
- **TanStack React Query 5.90.5** (React Query) — управление серверным состоянием, кэширование, синхронизация данных
- **Axios 1.13.0** — HTTP клиент для API запросов

### Работа с датами
- **Day.js 1.11.18** — легковесная библиотека для работы с датами и временем

### Инфраструктура разработки
- **React Scripts 5.0.1** — сборка и инструменты разработки (Create React App)
- **Webpack** — сборщик модулей (через react-scripts)

---

## ⚙️ Backend (Server-Side)

### Основные технологии
- **Node.js 18** — JavaScript runtime
- **TypeScript 5.9.3** — типизированный JavaScript
- **Express 5.1.0** — веб-фреймворк для Node.js

### База данных и ORM
- **PostgreSQL 15** — реляционная база данных
- **Prisma 6.18.0** — современный ORM и инструменты для работы с БД
  - Prisma Client — type-safe клиент для запросов
  - Prisma Migrate — миграции схемы БД
  - Prisma Studio — GUI для работы с данными

### Кэширование и сессии
- **Redis 7** — in-memory хранилище для кэширования и управления сессиями

### Безопасность и аутентификация
- **JSON Web Token (JWT) 9.0.2** — токены для аутентификации
- **bcryptjs 3.0.2** — хэширование паролей
- **Helmet 8.1.0** — установка security headers
- **express-rate-limit 8.1.0** — защита от DDoS и брутфорса
- **express-validator 7.3.0** — валидация входящих данных

### Middleware и утилиты
- **CORS 2.8.5** — Cross-Origin Resource Sharing
- **Compression 1.8.1** — gzip сжатие ответов
- **Morgan 1.10.1** — логирование HTTP запросов
- **dotenv 17.2.3** — управление переменными окружения

### Инструменты разработки
- **Nodemon 3.1.10** — автоматическая перезагрузка при изменении кода
- **ts-node 10.9.2** — запуск TypeScript файлов напрямую

---

## 🗄️ База данных (PostgreSQL)

### Основные сущности системы
- **Account** — управление пользователями и доступом
- **Room** — комнаты для бронирований
- **Service** — услуги/сервисы (SPA процедуры)
- **Guest** — гости клиники
- **Therapist** — терапевты/мастера
- **Booking** — бронирования/записи
- **ServiceCategory** — категории услуг
- **BookingPayment** — платежи по бронированиям
- **Session** — управление сессиями пользователей

### Особенности схемы
- Связи many-to-many через промежуточные таблицы
- Аудит изменений через BookingLog
- Гибкая система атрибутов для гостей и терапевтов
- Поддержка многоязычности через Language

---

## 🐳 Инфраструктура и DevOps

### Контейнеризация
- **Docker** — контейнеризация приложений
- **Docker Compose** — оркестрация контейнеров
  - Development конфигурация (`docker-compose.yml`)
  - Production конфигурация для Azure (`docker-compose.azure.yml`)

### Cloud платформы
- **Microsoft Azure** — облачная платформа для деплоя
  - Azure Container Instances (ACI)
  - Azure App Service
  - Azure Container Registry (ACR)
  - Azure Database for PostgreSQL
  - Azure Cache for Redis

### Production сборки
- **Nginx** — веб-сервер для статических файлов (frontend)
- Multi-stage Docker builds — оптимизация размера образов
- Health checks — мониторинг состояния контейнеров

---

## 🛠️ Инструменты разработки

### Языки программирования
- **TypeScript** — на всем стеке для type safety

### Package Managers
- **npm** — менеджер пакетов Node.js

### Code Quality
- **ESLint** — линтинг кода (через react-scripts)
- **TypeScript Compiler** — проверка типов на этапе компиляции

### Testing (настроено, но не активно используется)
- **Jest** — тестовый фреймворк
- **React Testing Library** — тестирование React компонентов

---

## 📦 Структура данных и API

### Стиль API
- **RESTful API** — REST архитектура
- **JSON** — формат обмена данными
- **JWT Bearer Token** — аутентификация через HTTP заголовки

### API Endpoints
- `/api/auth` — аутентификация и авторизация
- `/api/bookings` — управление бронированиями
- `/api/rooms` — управление комнатами
- `/api/services` — управление услугами
- `/api/guests` — управление гостями
- `/api/therapists` — управление терапевтами
- `/api/reservations` — расширенная система резерваций
- `/api/admin` — административные функции

---

## 🔐 Безопасность

### Аутентификация и авторизация
- JWT токены для сессий
- Хэширование паролей (bcryptjs)
- Ролевая система доступа (Account → Role → AccessRight)

### Защита
- Rate limiting — защита от злоупотреблений
- Helmet — security headers
- CORS — контроль доступа с других доменов
- Input validation — валидация всех входящих данных

---

## 📊 Особенности архитектуры

### Frontend
- **Component-based** — модульная архитектура React
- **Hooks** — функциональные компоненты с хуками
- **Context API** — глобальное состояние (AuthContext)
- **Custom hooks** — переиспользуемая логика

### Backend
- **MVC-подобная архитектура**:
  - Routes — маршрутизация
  - Controllers — бизнес-логика (через routes)
  - Models — через Prisma Client
  - Middleware — промежуточная обработка
- **Type-safe** — полная типизация через TypeScript и Prisma

### Стиль кода
- **TypeScript-first** — максимальное использование типов
- **Functional Programming** — функциональный подход в React
- **Async/Await** — современная работа с асинхронностью

---

## 🚀 Производительность

### Оптимизации
- **React Query кэширование** — автоматическое кэширование запросов
- **Code splitting** — разделение кода на чанки (через Webpack)
- **Gzip compression** — сжатие ответов сервера
- **Prisma connection pooling** — пул соединений с БД
- **Redis caching** — кэширование часто запрашиваемых данных

### Production оптимизации
- **Nginx static serving** — эффективная раздача статики
- **Multi-stage builds** — минимизация размера Docker образов
- **Production dependencies only** — исключение dev зависимостей

---

## 📝 Заключение

Это современное **Full-Stack TypeScript** приложение с:
- ✅ Type safety на всем стеке
- ✅ Современный React 19 с хуками
- ✅ Надежная ORM (Prisma)
- ✅ Масштабируемая архитектура
- ✅ Готовность к production (Docker, Azure)
- ✅ Безопасность из коробки
- ✅ Отличный Developer Experience

**Идеально подходит для:**
- Систем управления бронированиями
- SPA/Wellness центров
- Клиник и салонов красоты
- Любых систем с расписанием и ресурсами


